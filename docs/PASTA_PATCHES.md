# PASTA patches manifest

Authoritative list of every PASTA-specific patch on the vendored LLVM fork at `vendor/llvm-project/src` (currently branch `pasta-llvmorg-18.1.4`). Generated by `scripts/build_pasta_patches_manifest.py`; intent summaries are hand-curated.

- **Base ref:** `llvmorg-18-init`
- **Patch count:** **61**
- **Author filter:** `peter@trailofbits.com`, `peter.goodman@gmail.com`, or any address containing `kumarak`. (The plan's awk pattern `trailofbits.com` was broader and caught unrelated TOB upstream commits — see the script for the narrower filter actually used here.)

## Why this file exists

Every LLVM upgrade has to forward-port these patches onto the new base. The structural metadata (SHA, files, hash) is machine-extractable; the **intent summary** is the cross-version anchor — when a future cherry-pick fails because upstream refactored around the patch, the porter agent (Step 19 of the `snug-floating-finch` plan) re-derives the patch from the intent rather than from the literal diff.

Re-running the builder preserves any non-TODO intent text already in this file. Add or refine intents directly in this Markdown.

---

## `b9e223fb2e19` — Fix bug in clang where the operator location of MemberExpr is not correctly maintained through template specialization.

- **SHA:** `b9e223fb2e194e3963f922ba101ecb7d7a3fcbf7`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:72893a2dedfb4d64`
- **Files touched (1):**
  - `clang/lib/Sema/TreeTransform.h`

**Intent:**
Removes a `getMemberLoc()` rewrite in `TreeTransform.h` that was clobbering the operator location when a `MemberExpr` was rebuilt during template specialization. Preserves the user-written operator location through PASTA's traversal — the most recent PASTA patch on this fork.

## `ab9636a368a8` — Patch for sigsegv.

- **SHA:** `ab9636a368a805d3d7d1b452e0e3af04df66ba6d`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:f34456d7d3b984b2`
- **Files touched (1):**
  - `clang/lib/AST/ExprConstant.cpp`

**Intent:**
Extends `HandleSizeof`'s dependent-type early-return to also catch `isUndeducedType()` and `isUndeducedAutoType()`. PASTA hit a sigsegv when constant-folding `sizeof` of an undeduced type; the guard diagnoses cleanly instead.

## `664c37546def` — Add an extra check for undeduced auto types to prevent an assertion in debug builds / sigsegv in release.

- **SHA:** `664c37546def1b9e364bd1093702e35a7ed2a0f2`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:3061ae4e4496611c`
- **Files touched (1):**
  - `clang/lib/AST/Decl.cpp`

**Intent:**
Adds an `isUndeducedAutoType()` precondition before a downstream check in `Decl.cpp`. Stops a debug-build assertion / release-build sigsegv when an undeduced `auto` type slips through.

## `2bb33c07d50e` — Patch assertion related to looking up things. Make it possible to parse certain unfriendly linux kernel code.

- **SHA:** `2bb33c07d50e3c581b6afc28b0ffc2c07f187530`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:1192c47359d7a776`
- **Files touched (2):**
  - `clang/lib/Sema/SemaDecl.cpp`
  - `clang/lib/Sema/SemaLookup.cpp`

**Intent:**
Patches an assertion on the lookup path so PASTA can parse Linux kernel code that triggers Clang's stricter assumptions. Adds a tolerance in both `SemaDecl` and `SemaLookup`.

## `e1f6b3b0f605` — Undo most changes on SemaLambda, and then undo an old change, then disable CheckLDC.

- **SHA:** `e1f6b3b0f605db4ef17730c6a41271add02464a2`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:e0108669d2690f41`
- **Files touched (2):**
  - `clang/lib/AST/Decl.cpp`
  - `clang/lib/Sema/SemaLambda.cpp`

**Intent:**
Rolls back most of `d7267c23`'s `SemaLambda` changes, undoes an even older change in `Decl.cpp`, and disables `CheckLDC` outright. The net effect is to skip an over-restrictive lambda-decl-context check that fired on PASTA-instantiated lambdas.

## `d7267c239e5d` — Slightly broken fix for CheckLDC on lambdas

- **SHA:** `d7267c239e5dc64e0a9d0d4df58be3e20347e107`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:43f92ea558b60f6e`
- **Files touched (1):**
  - `clang/lib/Sema/SemaLambda.cpp`

**Intent:**
Partial fix for the `CheckLDC` (CheckLambdaDeclContext) path on lambdas. Author marks it as slightly broken; later patch `e1f6b3b0` rolls most of it back. Keep for context but expect to drop on port.

## `f36f80d987bd` — Put a substituted lambda from inside a class template into the TUs decl list. (#15)

- **SHA:** `f36f80d987bdf52624f0eaa3491f8545a8f61ef6`
- **Author:** `peter.goodman@gmail.com`
- **Line-range hash:** `sha256:09927ffabc2be419`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Splices a substituted lambda from inside a class template back into the translation unit's decl list (PR #15). Complements `10adf0a2`'s lambda-lexical-context change: covers the case where the lambda originates inside a class template.

## `0cb85ee30a78` — Adds missing function template specializations to their lexical decl contexts.

- **SHA:** `0cb85ee30a78e73caa9a11e812d9fa1b325e12e8`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:00a3b214b8159de2`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Adds missing function-template specializations to their lexical decl contexts in `InstantiateFunctionDefinition`. Without this, function-template specs instantiated under PASTA mode are reachable only from the template, not from the lexical scope they appeared in.

## `3da5cabd70d1` — Avoid nullptr deref.

- **SHA:** `3da5cabd70d16325b7f3254915c29791fbc60119`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:9181501cd6b15b2d`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplate.cpp`

**Intent:**
Avoids a nullptr deref in `ActOnExplicitInstantiation` when the templated decl has no definition: falls back to the templated decl itself rather than dereferencing `nullptr` from `getDefinition()`.

## `2d3968a45d5d` — More remapping of declarations.

- **SHA:** `2d3968a45d5d308ed8e9a73a6983ba07a8b621fe`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:371807208eb4ad03`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Propagates `RemappedDecl` through three more template-instantiation paths: `VisitClassTemplateDecl`, `VisitVarTemplateDecl`, and the function-template friend path inside `VisitFunctionDecl`. Continues the closure work started by `120ba829` and `f4755524`.

## `10adf0a234e8` — Set the lexical decl context of a lambda class to be the translation unit.

- **SHA:** `10adf0a234e8e3c9723969d229ab28c24be74b1e`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:5379c9a48a9dacb9`
- **Files touched (1):**
  - `clang/lib/Sema/SemaLambda.cpp`

**Intent:**
Sets the lexical decl context of a lambda class to the translation unit (rather than the enclosing function/scope). PASTA wants every lambda class to be reachable from the TU's decl list for whole-program traversal; without this, lambdas defined inside templates were invisible to top-level walks.

## `9f8e86a28143` — Source ranges on clang lambda classes.

- **SHA:** `9f8e86a28143c318cc1fadfc2687f746453438db`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:a3b256caeff7f4a4`
- **Files touched (1):**
  - `clang/lib/Sema/SemaLambda.cpp`

**Intent:**
Sets a meaningful source range on lambda classes in `SemaLambda` so PASTA's traversal can map the synthesized class back to the lambda-expression tokens the user wrote.

## `4b74d57ae939` — Add missing NEON intrinsics. Copied from https://reviews.llvm.org/D158626

- **SHA:** `4b74d57ae9393391b5f8fd4f49368bb9bc0d4e63`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:e3b0c44298fc1c14`
- **Files touched:** _(none)_

**Intent:**
Adds the missing NEON intrinsics from upstream Phabricator review D158626 (a fix that hadn't yet landed in `llvmorg-18.1.4`). PASTA users compiling NEON-using code on macOS hit undeclared-intrinsic errors without these.

## `f475552403d8` — Another remapped decl case.

- **SHA:** `f475552403d8b7c90c6b3c07eee29f541f58bcad`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:82d98e863d50cbc1`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Adds the `RemappedDecl` link for `VarTemplatePartialSpecializationDecl` in `InstantiateVarTemplatePartialSpecialization`. Variable template partial specs were missed by the earlier `RemappedDecl` patches; this closes a gap PASTA hit on var-template-heavy headers.

## `120ba8294a42` — Remap partial specializations instantiated from partial specializations.

- **SHA:** `120ba8294a4296ca03e667c6c5c506868220256c`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:6d930a9fc2b88188`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Adds the `RemappedDecl` link for *partial* specializations that are themselves instantiated from another partial specialization. Without this case, PASTA's downstream remapping loses provenance for one level of specialization indirection.

## `c968ae3b9d65` — Possible fix for template methods with auto return type

- **SHA:** `c968ae3b9d6592bee8b341046db2fb24f5848001`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:770e7f0828d3bf2d`
- **Files touched (1):**
  - `clang/lib/Sema/SemaExpr.cpp`

**Intent:**
Speculative defensive check in `SemaExpr` for template methods that have an `auto` (undeduced) return type. Prevents an assertion PASTA hit in real-world template code that exercised the path before deduction completed.

## `97864e8577ea` — Template arguments by reference

- **SHA:** `97864e8577ea7695b42abfec94fd2a0b765afaa8`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:3d9009ead3fbe551`
- **Files touched (4):**
  - `clang/include/clang/AST/ExprCXX.h`
  - `clang/include/clang/AST/Type.h`
  - `clang/lib/AST/ExprCXX.cpp`
  - `clang/lib/AST/Type.cpp`

**Intent:**
Switches a few APIs in `ExprCXX.h` and `Type.h` from passing `TemplateArgument` by value to by reference. PASTA's traversal code holds onto these args; by-value copies were both wasteful and subtly broke source-loc identity.

## `ba33dac188c6` — Patch for CXXOperatorCallExpr::getSourceRangeImpl.

- **SHA:** `ba33dac188c6ee6b3ee7a3661c083b85e2423fea`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:5c62685174b3c96c`
- **Files touched (1):**
  - `clang/lib/AST/ExprCXX.cpp`

**Intent:**
Patches `CXXOperatorCallExpr::getSourceRangeImpl` so the returned range covers the operator's tokens correctly when the call is infix vs. prefix. PASTA's wrappers expose source ranges to downstream tools; the previous bug made operator-call ranges unreliable.

## `78c138a514e5` — Attempt to fix issue with static constexpr out-of-line methods where there was double specialization of out-of-line definition.

- **SHA:** `78c138a514e56096c38f4d99943726ad3aaddb9d`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:9eccdb998879266b`
- **Files touched (2):**
  - `clang/lib/Sema/SemaOverload.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Avoids double-specialization of out-of-line definitions for `static constexpr` methods. When PASTA's lexical instantiation runs, the out-of-line definition was being instantiated twice; this patch checks the overload set in `SemaOverload` to break the duplicate instantiation cycle.

## `24f9ad2bf11f` — Add function CheckSetLocation, called in debug mode on every call to Decl::setLocation. Can be useful to add checks inside for breakpoints. Removed a ->setLocation on a FunctionDecl that was causing bounds issues.

- **SHA:** `24f9ad2bf11fa359b5c9c26611a976a4b7bbd649`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:cfafddc148a11304`
- **Files touched (4):**
  - `clang/include/clang/AST/DeclBase.h`
  - `clang/lib/AST/DeclBase.cpp`
  - `clang/lib/Sema/SemaTemplate.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Adds a `Decl::CheckSetLocation` debug hook called from every `Decl::setLocation`, providing a single breakpoint location for tracking down location-rewrite bugs. Also removes a `->setLocation` call on a `FunctionDecl` in `SemaTemplate` that was rewriting the user-facing location with a synthesized one.

## `ba3023ca5201` — Change TSI initialization for methods. More declcontext checking

- **SHA:** `ba3023ca5201ce8376cd50536ac312cacf19b45e`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:e6711dc7d85aa2b7`
- **Files touched (4):**
  - `clang/lib/AST/Decl.cpp`
  - `clang/lib/AST/DeclBase.cpp`
  - `clang/lib/AST/DeclCXX.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Two-part change: improves `TypeSourceInfo` initialization for methods (so the instantiated method carries the right TSI rather than synthesizing a fresh one), plus more `DeclContext`-validity checks in `DeclBase`/`DeclCXX` to handle PASTA's looser invariants during lexical-template instantiation.

## `ea131dfc42f5` — Don't overwrite source location of canonical decl

- **SHA:** `ea131dfc42f5f5b2bf0b59c9b699e1bc2e6bb7a3`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:e8dcf0e79af5b620`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplate.cpp`

**Intent:**
Stops `SemaTemplate` from overwriting the source location of a canonical decl during specialization. PASTA wants the canonical decl's location to remain the user-written source location, not the instantiation's synthesized one.

## `c311dd64d49a` — Extern template patch, and some changes to method instantiation

- **SHA:** `c311dd64d49a159c2009806da7a326b52b059f63`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:5016a84991eb98bb`
- **Files touched (9):**
  - `clang/lib/AST/Decl.cpp`
  - `clang/lib/AST/DeclBase.cpp`
  - `clang/lib/Sema/SemaDecl.cpp`
  - `clang/lib/Sema/SemaDeclCXX.cpp`
  - `clang/lib/Sema/SemaLambda.cpp`
  - `clang/lib/Sema/SemaOverload.cpp`
  - `clang/lib/Sema/SemaTemplate.cpp`
  - `clang/lib/Sema/SemaTemplateDeduction.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Multi-file patch covering extern-template handling and method instantiation. Touches `Decl`, `DeclBase`, `SemaDecl`, `SemaDeclCXX`, `SemaLambda`, `SemaOverload`, `SemaTemplate`, `SemaTemplateDeduction`, and `SemaTemplateInstantiateDecl` (~134 lines). Restructures how methods are instantiated so PASTA can see the template-defined-but-extern instantiations the user code depends on.

## `014c936170cb` — Defer instantiation fix

- **SHA:** `014c936170cb0499bb35bca4dcf55b2c4fd91bb4`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:a9ee875e2140536c`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiate.cpp`

**Intent:**
Removes the `Function->isOutOfLine()` precondition before adding to `PendingInstantiations`. With LexicalTemplateInstantiation on, even in-line definitions need to be deferred (the lexical-vs-effective distinction matters for PASTA's traversal).

## `d506d1cf350b` — Nullptr check

- **SHA:** `d506d1cf350bbb64c821bf41663d878fd75aa2d5`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:787a1572154b6af0`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiate.cpp`

**Intent:**
Replaces two `dyn_cast<...>(NDecl)` calls with `dyn_cast_or_null` in `InstantiateClass`'s remapped-decl update path. NDecl can be null in PASTA-mode template instantiation; without the null-tolerant cast the process crashes.

## `0d2cc7e30990` — Tweaks the definition of FunctionDecl and VarDecl ::isOutOfLine to not go and discover if the primary thing is out-of-line, but to just focus on the current decl

- **SHA:** `0d2cc7e309902c5e09e36c959a4753d1de7e789f`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:27b3c536495fbc32`
- **Files touched (1):**
  - `clang/lib/AST/Decl.cpp`

**Intent:**
Tweaks `FunctionDecl::isOutOfLine` and `VarDecl::isOutOfLine` so they consider only the current decl rather than walking back to the primary template's notion of out-of-line-ness. PASTA's wrappers need the local answer; the redirect produced wrong results.

## `a2a017e5c31f` — Speculative fix for a bug I saw with enable_if_not_same

- **SHA:** `a2a017e5c31f40b5c74688619217710654339eb9`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:601f9fe55636f7d7`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Removes a guarding condition in `PerformDeferredTypeCompletions` that skipped `TransferLexicalInfo` when partial specialization had no definition or location-mismatched. Speculative — author calls it out as such; suspicious behavior with `enable_if_not_same`. Review carefully when porting.

## `20f1a9232cd8` — Simpler declcontext casting.

- **SHA:** `20f1a9232cd8767f6802c944dce08f9335bf0eb5`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:7966ae790b0890cd`
- **Files touched (1):**
  - `clang/lib/AST/DeclBase.cpp`

**Intent:**
Simplifies `DeclContext` casting paths in `DeclBase`. PASTA downstream uses `DeclContext` casts intensively; the cleaner version avoids a surprising fallthrough that returned the wrong context.

## `328fc70cc264` — Alter inject class patch to solve argument deduction bug. Now relying on decl remapping to help multiplier avoid the issue.

- **SHA:** `328fc70cc2640b65f9ff53e56b60660efbc2a72e`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:80ede400d541b0e8`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDecl.cpp`

**Intent:**
Reduces an earlier inject-class patch in `SemaDecl` after switching the canonical fix to live in decl-remapping infrastructure. Argument deduction bug is now solved by the remapping path; this patch deletes the now-redundant fallback.

## `c2a3e0bcbedc` — Disable assertion when processing of predefines is disabled.

- **SHA:** `c2a3e0bcbedc0edfd396f90369ea522e9a0c904f`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:f953b373fb3c8e67`
- **Files touched (1):**
  - `clang/lib/Lex/PPLexerChange.cpp`

**Intent:**
Disables a `PPLexerChange` assertion when predefines processing is off. Required because `-fdisable-macro-preamble` (patch `5254363b`) lets users skip predefines, and the assertion assumes they ran.

## `7985c65dd551` — Defer instantiation of out-of-line methods. When an overload set is ambiguous, choose the first thing.

- **SHA:** `7985c65dd55143b7c6dfbe07b85f9ba3ee584646`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:daaf8be74615135b`
- **Files touched (2):**
  - `clang/lib/Sema/SemaOverload.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiate.cpp`

**Intent:**
Two changes: (1) defers instantiation of out-of-line methods rather than instantiating eagerly during overload resolution; (2) when an overload set is genuinely ambiguous, picks the first candidate instead of erroring. Both reduce the number of bail-outs PASTA hits on real-world C++ code that Clang would otherwise refuse.

## `332678726b8d` — Fix for build/installation of libc++

- **SHA:** `332678726b8dca79937394dc5fb6d7f65306ae6f`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:64c1de70cd72eb1a`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Relaxes a `Function->getLocation() == PatternDecl->getLocation()` assertion in `InstantiateFunctionDefinition` to only fire when `LexicalTemplateInstantiation` is OFF. The PASTA mode intentionally diverges these locations; without the gate, libc++ build trips the assertion.

## `fb7bed8ce4fa` — Improve r_angle visibility.

- **SHA:** `fb7bed8ce4fa52835abe6cd4e248c3b1dd932ea0`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:1de88a6ad6e8e56d`
- **Files touched (1):**
  - `clang/lib/Parse/ParseTemplate.cpp`

**Intent:**
Improves visibility-tracking of the closing `>` token in template argument lists in `ParseTemplate`. Builds on `fdfbbfd7`'s split-token visibility infrastructure.

## `1673270c7849` — Address PR feedback

- **SHA:** `1673270c78493d8a85ec16f334ac849d07386afd`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:a7e77839ee8dcd68`
- **Files touched (3):**
  - `clang/include/clang/AST/DeclTemplate.h`
  - `clang/lib/AST/DeclTemplate.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Addresses upstream PR review feedback on the source-range patches: uncomments the `getSourceRange()` override on `ClassTemplateSpecializationDecl`, tightens the `getSpecializedTemplate` walk, and removes a no-longer-needed duplicate hunk in `SemaTemplateInstantiateDecl`.

## `1895aa95f4d0` — Sometimes the rangle can be a comma. Make more astcontext methods public as pasta will have a better representative location for decl locations for comment searching than clang will.

- **SHA:** `1895aa95f4d092a7c795114351d4fc10b16bec30`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:7cbe001290847d24`
- **Files touched (2):**
  - `clang/include/clang/AST/ASTContext.h`
  - `clang/lib/Parse/ParseTemplate.cpp`

**Intent:**
Two unrelated tweaks: (1) `ParseTemplate` accepts a comma in place of an r-angle when looking for the end of a template arg list (handles an edge case PASTA's recovery surfaced); (2) makes a couple of `ASTContext` methods public so PASTA — which has more accurate decl-location info than Clang for comment searching — can call them.

## `5254363b3617` — Add an option to disable the macro preamble.

- **SHA:** `5254363b3617c28bbf1c12171efae990ff8daf56`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:125a6b412f348d06`
- **Files touched (2):**
  - `clang/include/clang/Basic/LangOptions.def`
  - `clang/lib/Lex/Preprocessor.cpp`

**Intent:**
Adds a `-fdisable-macro-preamble` LangOpt + Preprocessor flag. PASTA fixtures and tooling sometimes need to skip the implicit macro preamble (predefined-macros block) to control what's parsed.

## `3b727ad1ec45` — Changes to handle split token visibility, as well as lexical template instantiation corner cases, and attribute printing bugs.

- **SHA:** `3b727ad1ec45f22958e6f8a4068b197ddc649891`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:5ed9a7ddb8a96740`
- **Files touched (25):**
  - `clang/include/clang/AST/Attr.h`
  - `clang/include/clang/AST/Decl.h`
  - `clang/include/clang/AST/DeclBase.h`
  - `clang/include/clang/AST/DeclTemplate.h`
  - `clang/include/clang/AST/PrettyPrinter.h`
  - `clang/include/clang/Basic/LangOptions.def`
  - `clang/include/clang/Driver/Options.td`
  - `clang/include/clang/Lex/PPCallbacks.h`
  - `clang/include/clang/Lex/PPCallbacksEventKind.h`
  - `clang/include/clang/Sema/Sema.h`
  - `clang/lib/AST/AttrImpl.cpp`
  - `clang/lib/AST/DeclTemplate.cpp`
  - `clang/lib/AST/StmtPrinter.cpp`
  - `clang/lib/Lex/PPCaching.cpp`
  - `clang/lib/Parse/ParseDeclCXX.cpp`
  - `clang/lib/Parse/ParseTemplate.cpp`
  - `clang/lib/Sema/Sema.cpp`
  - `clang/lib/Sema/SemaCXXScopeSpec.cpp`
  - `clang/lib/Sema/SemaDecl.cpp`
  - `clang/lib/Sema/SemaExpr.cpp`
  - `clang/lib/Sema/SemaTemplate.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiate.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`
  - `clang/lib/Sema/TreeTransform.h`
  - `clang/utils/TableGen/ClangAttrEmitter.cpp`

**Intent:**
Large composite patch: extends the split-token visibility plumbing to AttrImpl/DeclTemplate/Lex/Sema, fixes corner cases in lexical template instantiation (~430 lines in `SemaTemplateInstantiateDecl`), and addresses several attribute-printing bugs surfaced by PASTA. Builds on patches `e2850ae6` and `fdfbbfd7`.

## `fdfbbfd7a229` — Visibility on token splits.

- **SHA:** `fdfbbfd7a2290be0e8ee9cf7d52a1227942f9d04`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:30ecac6161fed460`
- **Files touched (4):**
  - `clang/include/clang/Lex/PPCallbacks.h`
  - `clang/include/clang/Lex/PPCallbacksEventKind.h`
  - `clang/include/clang/Lex/Preprocessor.h`
  - `clang/lib/Parse/ParseTemplate.cpp`

**Intent:**
Threads visibility information through the `TokenSplit` PPCallback event and `ParseTemplate`. Lets PASTA distinguish split tokens that are visible (originating from user code) from those produced by preprocessor expansion machinery — preserves source fidelity for downstream consumers.

## `1280e9c1a1e5` — Totally disable the warning about builtin decls.

- **SHA:** `1280e9c1a1e5d20fb5dff14230719d4094133e27`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:5ef8003ed82569aa`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDecl.cpp`

**Intent:**
Removes the diagnostic Clang emits when a builtin is redeclared. PASTA fixtures intentionally redeclare builtins for testing, and the warning is noise in those flows.

## `c94deefd0500` — Don't error on redefinition of some builtins

- **SHA:** `c94deefd050021e09f0cd7f1b478ddf20d7444a1`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:64c74f68feea83ac`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDecl.cpp`

**Intent:**
Stops `SemaDecl` from erroring when user code redefines specific builtins (extends the earlier `~`-prefix logic to a few stock builtins PASTA fixtures need to override). Note the related but different `1280e9c1` patch that disables the warning entirely.

## `9b681b8f7fb1` — Bug fix.

- **SHA:** `9b681b8f7fb172b8abe4e438928a4b4910058a36`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:358104aac1d06ddb`
- **Files touched (1):**
  - `clang/lib/Lex/PPLexerChange.cpp`

**Intent:**
Null-checks `Callbacks` before firing the `EndMacroExpansion` event in `Preprocessor::HandleEndOfTokenLexer`. PASTA configurations sometimes construct a Preprocessor without callbacks attached, and the original code segfaulted on every macro-expansion end.

## `bfc565aa41ae` — Bug fix in clang for not constant folding unevaluated strings in attributes.

- **SHA:** `bfc565aa41aee8eb1e2a8f11db3ad721e03a1106`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:7890a4b458012e84`
- **Files touched (1):**
  - `clang/lib/Sema/SemaAttr.cpp`

**Intent:**
Constant-folds unevaluated string literals that appear in attribute arguments before storing them on the AST node. Without this, PASTA sees the unevaluated form (with placeholder ranges) and downstream consumers can't resolve the string.

## `ae76c00abef5` — Typo

- **SHA:** `ae76c00abef5afca5ed693a826b18d5db45d7ef8`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:07723bc10d8740c0`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDeclAttr.cpp`

**Intent:**
One-character typo fix in the previous patch: the zero-arg check used the wrong local (`L.getNumArgs()` → `AL.getNumArgs()`).

## `942990b6f780` — More annotation stuff.

- **SHA:** `942990b6f78074acbcc4afd0d5681fe1f699a350`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:4ad0a6ec9ae07fa2`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDeclAttr.cpp`

**Intent:**
Extends `handleAnnotateAttr`'s unknown-attr fallback to cover the zero-argument case as well: an empty `[[foo]]` should still surface as an annotation attr instead of being dropped.

## `fdd12904c57c` — More moving code.

- **SHA:** `fdd12904c57c80dc343486443743ad62798a8924`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:e3b0c44298fc1c14`
- **Files touched:** _(none)_

**Intent:**
Empty commit (no diff). Likely a checkpoint commit during interactive rebase; safe to drop on forward-port.

## `9cc843aba481` — Fix another path through annotations. This seems like we're double adding attributes again, so this fix might not be great.

- **SHA:** `9cc843aba48105222092bcd71a19030029b3ee9d`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:ca963511756a3b84`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDeclAttr.cpp`

**Intent:**
Adds an early-return path in `handleAnnotateAttr` so non-expr arguments of a parsed attribute are routed through the unknown-attr-as-annotate handler when `UnknownAttrAnnotate` is on. The author flags this as a possibly-double-add fix; review when porting.

## `6e5f9ea7dd1f` — Fixes inverted check.

- **SHA:** `6e5f9ea7dd1f7065c3dd895bc3405217ff46a80a`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:754c4870cd613b7c`
- **Files touched (1):**
  - `clang/lib/Sema/SemaTemplateInstantiate.cpp`

**Intent:**
Inverts an `if (!getLangOpts().LexicalTemplateInstantiation)` check in `InstantiateClassTemplateSpecialization` — the PASTA codepath should fire WHEN the option is ON, not when it's off. Without this fix, the template-specialization remapping infrastructure runs in stock-Clang mode and breaks normal builds.

## `d0261ff24614` — Undo porting a patch from cxx-common that breaks normal builds.

- **SHA:** `d0261ff24614f18ebc358936c83f2102ea9381e7`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:227ea408189ed30f`
- **Files touched (1):**
  - `cmake/Modules/FindPrefixFromConfig.cmake`

**Intent:**
Reverts the `CMAKE_CURRENT_LIST_DIR` half of the previous CMake patch because it broke regular (non-cxx-common) installs. The Z3 changes are kept; only the `find_prefix_from_config` line returns to `CMAKE_CURRENT_LIST_FILE`.

## `e7af1e5837a6` — CMake patches

- **SHA:** `e7af1e5837a61478f673b34307aac21632980a56`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:55e81447fa865988`
- **Files touched (2):**
  - `cmake/Modules/FindPrefixFromConfig.cmake`
  - `llvm/cmake/modules/FindZ3.cmake`

**Intent:**
CMake patches: switches `find_prefix_from_config` to use `CMAKE_CURRENT_LIST_DIR` (more correct under symlinks) and rewrites `FindZ3.cmake` to prefer Z3's upstream config-mode package before falling back to manual probing. Both improve config robustness when PASTA is built against a non-standard install layout.

## `3eeda5245b3c` — If a builtin id is a pasta builtin, then allow code to redefine the builtin.

- **SHA:** `3eeda5245b3c80629dedafb9985b40974dc556b8`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:1388668a0bc3387c`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDecl.cpp`

**Intent:**
Lets user code redefine PASTA-private (`~`-prefixed) builtins. PASTA-side fixtures need to provide their own definitions; without this, `SemaDecl` rejects the redeclaration as a builtin clash.

## `2a96642d736a` — Adds a custom '~' attribute to builtins that can be recognized as pasta-specific builtins.

- **SHA:** `2a96642d736a04977da022d779e71423dc9e3dd6`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:c0a2bc8d7ae8ec3f`
- **Files touched (2):**
  - `clang/include/clang/Basic/Builtins.h`
  - `clang/lib/Sema/SemaDecl.cpp`

**Intent:**
Adds the `~` prefix convention to `Builtins.def` for PASTA-private builtins, plus a `Builtins::isPastaBuiltin` query. Lets PASTA inject private builtins (queried later by `SemaDecl`) without colliding with Clang's reserved namespace.

## `0fa5ef8706f0` — Treat invalid attributes as annotation attributes

- **SHA:** `0fa5ef8706f073c00daba81a5d83e1984a7c5348`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:84d3e2919380dd4a`
- **Files touched (1):**
  - `clang/lib/Sema/SemaDeclAttr.cpp`

**Intent:**
Routes invalid (failed-to-parse) attributes through the same annotation-attribute handler unknown attributes use, so PASTA still sees them in the AST instead of dropping them silently.

## `e5a3dcf7fd1d` — Improve how we specify the command-line options.

- **SHA:** `e5a3dcf7fd1dfdca007f3055dc9de9b704454e83`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:ed18cd8d401d2b98`
- **Files touched (1):**
  - `clang/include/clang/Driver/Options.td`

**Intent:**
Cosmetic refactor of the PASTA-added driver flags in `Options.td` so they share help text formatting and group naming.

## `ff860c14f274` — Fix up the bug related to unsupported attributes. So far not able to precisely distinguish what should be a decl or a type attribute, so all unknown attributes are converted to decl attributes.

- **SHA:** `ff860c14f27483e8c6434df17d5183f98ff63cbd`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:bb427a828c2645f3`
- **Files touched (8):**
  - `clang/include/clang/Basic/ParsedAttrInfo.h`
  - `clang/include/clang/Sema/ParsedAttr.h`
  - `clang/lib/AST/ASTContext.cpp`
  - `clang/lib/Sema/ParsedAttr.cpp`
  - `clang/lib/Sema/SemaAttr.cpp`
  - `clang/lib/Sema/SemaDeclAttr.cpp`
  - `clang/lib/Sema/SemaType.cpp`
  - `clang/utils/TableGen/ClangAttrEmitter.cpp`

**Intent:**
Tightens the unknown-attribute-as-annotation handling: extends `ParsedAttrInfo`/`ParsedAttr` so unknown attrs can be classified as decl-attrs explicitly (instead of guessing decl-vs-type), and updates `SemaDeclAttr`/`SemaType` to route them. PASTA loses fewer attributes but, per the message, all unknown attrs still default to decl-attr until a precise rule emerges.

## `0203bf1566cf` — Fix compiler errors.

- **SHA:** `0203bf1566cf5ff1daca609cdde04328018b9294`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:d83316aeda9ffa6a`
- **Files touched (1):**
  - `clang/lib/AST/ASTContext.cpp`

**Intent:**
Compile fixes for the previous `getConstantArrayType` change: corrects the loop variable from `PrevSizeExpr` to `OrigSizeExpr` and updates the `ConstantExpr` storage-kind enum to its renamed C++17 form (`ConstantResultStorageKind::None`).

## `56f63b20fa02` — Multiplier issue 427

- **SHA:** `56f63b20fa020c9baf8a5bdc5c860161dd7d60fb`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:ae26f4c3c2a4b989`
- **Files touched (1):**
  - `clang/lib/AST/ASTContext.cpp`

**Intent:**
Multiplier issue 427: changes how `ASTContext::getConstantArrayType` wraps array-size expressions. Previously bare integer literals were wrapped in `ConstantExpr` (which broke type deduplication); now they are kept as the literal, and existing `ConstantExpr`s are filled in with size info if missing. Preserves token provenance for array sizes while keeping deduplication semantics.

## `e2850ae654bc` — Changes for maintaining lexical provenance of template specializations

- **SHA:** `e2850ae654bc8798b5bb6add1038755938a7b3ad`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:ca67c7dfd1b42cae`
- **Files touched (9):**
  - `clang/include/clang/Basic/LangOptions.def`
  - `clang/include/clang/Driver/Options.td`
  - `clang/include/clang/Sema/Sema.h`
  - `clang/include/clang/Sema/Template.h`
  - `clang/lib/Driver/ToolChains/Clang.cpp`
  - `clang/lib/Lex/Preprocessor.cpp`
  - `clang/lib/Sema/SemaDecl.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiate.cpp`
  - `clang/lib/Sema/SemaTemplateInstantiateDecl.cpp`

**Intent:**
Adds the `LexicalTemplateInstantiation` LangOpt + the bulk of the machinery in `SemaTemplateInstantiateDecl` that preserves the *lexical* origin of template specializations rather than the instantiation's synthesized location. Many later PASTA patches gate behavior on `getLangOpts().LexicalTemplateInstantiation`. Required for PASTA's source-location fidelity through templates.

## `185efa61c0fb` — Main set of pasta patches for the preprocessor and a variety of type issues.

- **SHA:** `185efa61c0fbdb71d7b67513d1d03d4c2b7da49c`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:e81cb290f281e387`
- **Files touched (29):**
  - `clang/include/clang/AST/PrettyPrinter.h`
  - `clang/include/clang/Basic/LangOptions.def`
  - `clang/include/clang/Basic/TokenKinds.def`
  - `clang/include/clang/Driver/Options.td`
  - `clang/include/clang/Lex/PPCallbacks.h`
  - `clang/include/clang/Lex/PPCallbacksEventKind.h`
  - `clang/include/clang/Lex/Preprocessor.h`
  - `clang/include/clang/Lex/TokenLexer.h`
  - `clang/lib/AST/ASTContext.cpp`
  - `clang/lib/AST/ExprConstant.cpp`
  - `clang/lib/AST/ItaniumMangle.cpp`
  - `clang/lib/AST/Type.cpp`
  - `clang/lib/Basic/SourceManager.cpp`
  - `clang/lib/Driver/ToolChains/Clang.cpp`
  - `clang/lib/Lex/Lexer.cpp`
  - `clang/lib/Lex/PPDirectives.cpp`
  - `clang/lib/Lex/PPExpressions.cpp`
  - `clang/lib/Lex/PPLexerChange.cpp`
  - `clang/lib/Lex/PPMacroExpansion.cpp`
  - `clang/lib/Lex/Pragma.cpp`
  - `clang/lib/Lex/Preprocessor.cpp`
  - `clang/lib/Lex/TokenLexer.cpp`
  - `clang/lib/Parse/ParseExpr.cpp`
  - `clang/lib/Parse/ParseTemplate.cpp`
  - `clang/lib/Sema/SemaDecl.cpp`
  - `clang/lib/Sema/SemaExpr.cpp`
  - `clang/lib/Sema/SemaExprCXX.cpp`
  - `clang/lib/Sema/TreeTransform.h`
  - `clang/utils/TableGen/ClangAttrEmitter.cpp`

**Intent:**
Large preprocessor + type-system overhaul. Adds the `PPCallbacksEventKind` enum + per-event callbacks so PASTA can observe every preprocessor event (macro expansion phases, token splits, pragma processing, etc.) instead of just the coarse defaults. Also touches `ASTContext`/`Type`/Sema to preserve token provenance through more constructs. The largest PASTA patch by line count and the basis many later patches depend on.

## `b7367f3350d1` — Convert unknown attributes to annotations. Embed attributes into AttributedTypes.

- **SHA:** `b7367f3350d1da81f066b246f2cabd5539f6530a`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:eef151877b8c3695`
- **Files touched (12):**
  - `clang/include/clang/AST/ASTContext.h`
  - `clang/include/clang/AST/Type.h`
  - `clang/include/clang/Basic/LangOptions.def`
  - `clang/include/clang/Driver/Options.td`
  - `clang/lib/AST/ASTContext.cpp`
  - `clang/lib/AST/ASTImporter.cpp`
  - `clang/lib/AST/Type.cpp`
  - `clang/lib/Driver/ToolChains/Clang.cpp`
  - `clang/lib/Parse/ParseDeclCXX.cpp`
  - `clang/lib/Sema/SemaDeclAttr.cpp`
  - `clang/lib/Sema/SemaType.cpp`
  - `clang/lib/Sema/TreeTransform.h`

**Intent:**
Foundational PASTA patch: makes Clang treat unknown C++ attributes as annotation attributes (instead of dropping them with a warning) and embeds attribute information into `AttributedType` nodes. Adds a `-funknown-attribute-annotate` LangOpt + driver flag. PASTA needs to preserve every attribute the user wrote even when Clang doesn't know the spelling, so downstream tools see them in the AST.

## `df7fc678053f` — Don't attempt to find macro expansions if there isn't a valid source location.

- **SHA:** `df7fc678053f6313e07a62ed3e2e7d639d9fa652`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:7ff61ab17e90558d`
- **Files touched (1):**
  - `clang/lib/AST/DeclBase.cpp`

**Intent:**
Skips macro-expansion lookup in `DeclBase` when the source location is invalid. Without the guard, PASTA's deeper traversal of declarations with synthesized/no-loc origins crashes inside the SourceManager.

## `fe27f9b1a582` — Fixes a bug in the tablegen for pretty printing Clang attributes which caused a null pointer dereference.

- **SHA:** `fe27f9b1a582fa13f717d51caefbcc9c85f41765`
- **Author:** `peter@trailofbits.com`
- **Line-range hash:** `sha256:50e4d42af044a2b0`
- **Files touched (1):**
  - `clang/utils/TableGen/ClangAttrEmitter.cpp`

**Intent:**
Hardens `ClangAttrEmitter`'s tablegen output against null pointer dereferences when pretty-printing Clang attributes that have null argument slots. PASTA exercises pretty-printing on a wider class of attributes than upstream tests cover, so this defensive check matters for PASTA's wrapper layer and is upstream-safe.

